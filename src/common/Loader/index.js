import React from 'react';
import './index.scss';

/**
 * Loader component.
 * @returns {String} The HTML markup for the loader component
 */
const Loader = () => (
  <div className="loader__container">
    <div className="loader" />
  </div>
);

export default Loader;
